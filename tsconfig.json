{
  "compilerOptions": {
    /* Specify ECMAScript target version */
    "target": "ES2017",
    /* Specify library files to be included in the compilation */
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    /* Allow JavaScript files to be compiled */
    "allowJs": true,
    /* Skip type-checking of declaration files */
    "skipLibCheck": true,
    /* Enable all strict type-checking options */
    "strict": true,
    /* Do not emit output; Next.js handles transpilation */
    "noEmit": true,
    /* Emit additional JS for better default import interoperability */
    "esModuleInterop": true,
    /* Specify module code generation */
    "module": "esnext",
    /* Specify module resolution strategy */
    "moduleResolution": "bundler",
    /* Allow importing .json files as modules */
    "resolveJsonModule": true,
    /* Ensure each file can be transpiled without relying on other imports */
    "isolatedModules": true,
    /* Keep JSX as-is (Next.js will handle it) */
    "jsx": "preserve",
    /* Enable incremental compilation */
    "incremental": true,
    /* Next.js plugin for improved editor support */
    "plugins": [
      {
        "name": "next"
      }
    ],
    /*
      Path aliases:
      - "@/..." → "src/..."
      Now e.g. `import X from '@/components/X'` points to `src/components/X`.
    */
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "src/*"
      ]
    }
  },
  /*
    Include:
    - next-env.d.ts (auto-generated by Next.js)
    - All .ts/.tsx files anywhere
    - The "src" folder (where your components, pages, etc. live)
    - Any custom "types" folder
    - next.config.js so it’s not flagged
  */
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "next-env.d.ts",
    "next.config.js",
    "src",
    "types",
    ".next/types/**/*.ts"
  ],
  /* Exclude node_modules and the Next.js build output */
  "exclude": [
    "node_modules",
    ".next"
  ]
}
